<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="author" content="gthole">

    <title>Gallery</title>

    <link href="/static/css/nprogress.css" rel="stylesheet">
    <link href="/static/css/style.css<% if @version %>?v=<%= @version %><% end %>" rel="stylesheet">
  <% if @gname and @image %>
    <link href="<%= @host %>/galleries/<%= @gname %>/_thumbs/300/<%= @image %>" rel="image_src" />
    <meta property="og:image" content="<%= @host %>/galleries/<%= @gname %>/_thumbs/300/<%= @image %>" />
  <% end %>

  <% if @version %>
    <script src="/static/js/script.js?q=<%= @version %>"></script>
  <% else %>
    <script data-main="/static/js/config" src="/static/js/libs/requirejs/require.js"></script>
  <% end %>
  </head>
  <body>
      <div class="nav">
        <span class="nav-title"><a href="/">Gallery</a><% if @fname %> / <%= @fname %><% end %></span>
      </div>
      <div class="page"></div>
      <script class="bootstrapData" type="data"><%= @data.to_json %></script>
  </body>
</html>
